import React from 'react';

const Loading = (): JSX.Element => {
  return (
    <div className="loading w-16 h-16 border-4 border-t-4 border-primary border-opacity-50 rounded-full animate-spin"></div>
  );
};

export default Loading;
